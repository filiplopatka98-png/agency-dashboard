// PLACEHOLDER — plná implementácia príde v kroku 7.
// tls.connect(443, host, { servername: host }) → getPeerCertificate() → valid_to
// → zápis do Supabase REST so service_role kľúčom (GitHub secret).
console.log('tls-probe: not implemented yet (krok 7)');
