/**
 * Vygenerované Supabase typy sa doplnia po kroku 2 cez `pnpm --filter @agency/db gen-types`
 * (potrebuje bežiaci lokálny Supabase). Do tej doby re-exportujeme placeholder.
 */
export type { Database } from './types.generated';
