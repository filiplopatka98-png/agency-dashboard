# UI potrebuje LEN tieto dve premenné (anon key + URL). Nič viac.
# Ak sa v apps/web objaví SERVICE_ROLE, je to bug.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
